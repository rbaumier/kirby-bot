{
  "name": "afk-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.3.0"
  },
  "description": "AFK orchestrator — deterministic state machine driving Claude Code through GitLab issues.",
  "scripts": {
    "test": "bun test src/",
    "typecheck": "tsc --noEmit",
    "setup-skills": "bun scripts/setup-skills.ts",
    "analytics": "bun scripts/analytics.ts"
  },
  "dependencies": {
    "@effect/platform": "^0.96.1",
    "@effect/platform-bun": "^0.89.0",
    "effect": "^3.12.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.7.0"
  }
}
